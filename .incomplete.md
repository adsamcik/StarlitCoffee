# Incomplete work — bag-scan LLM extraction

Status as of 2026-07-02 (session 4). All work below is **committed and pushed**;
this file tracks what is **not yet done** so the next session can resume.

## Session 4 — broad hallucination fix (origin/region/process/dates)

Commit on `main`: `b89a051`.

Followed up on session 3's top-priority finding (broad hallucination on the
9-bag new-samples subset). Root-caused via the ACTUAL hallucinated values
(pulled the raw per-bag JSONL, not just aggregate %):
- `origin='Arabica'` (a species, not a country)
- `region='Guatemala'`/`'Ethiopia'` (country duplicated into region)
- `region='PRAHA'` (a city/business address) / `region='Letni Sad'` (the
  bag's own product name)
- `process='Whole Bean'` (bean-form/packaging word, not a processing method
  — the existing prompt prohibition wasn't sticking)
- `expiryDate='3 months from roast date'` (not even a date)
- `variety='Arabica'` (bare species, not a distinguishing cultivar)

**Fix (both a code layer and a prompt layer, deliberately — prompt alone
wasn't holding for `process`, and dates had NO code-level guard at all):**
- `CoffeeMetadataNormalizer.sanitizeExtraction`: new Rule 0 drops bare
  species names (Arabica/Robusta) from origin/variety and bean-form words
  (whole bean/ground/...) from processType, BEFORE the existing cross-field
  reclassification rule runs (so a bad value is dropped, not relocated
  between slots).
- New `CoffeeMetadataNormalizer.sanitizeDate()`: delegates to the existing
  `DateParser` (already recognizes ISO/dd.MM.yyyy/month-name/etc.) so a
  relative phrase or garbled token is rejected without regressing real
  non-ISO dates. Wired into `BagPhotoScanSupport.buildPrefill` for
  roastDate/expiryDate — **these had zero sanitization before this fix**
  (unlike origin/region/process/variety, which already went through
  `sanitizeExtraction`).
- **Found + fixed a benchmark-realism gap along the way**: the benchmark
  scored raw pre-sanitization model output — production already runs
  origin/region/process/variety through `sanitizeExtraction` via
  `BagPhotoScanSupport.buildPrefill`, but the benchmark never called it, so
  it was measuring worse-than-real accuracy for those fields (and, for
  dates, coincidentally accurate since production had no date sanitization
  either, until now). `FullPipelineBenchmarkTest` now applies the same
  sanitization before scoring so it reflects what a user actually sees.
- Prompt hardening (`VISION_SYSTEM_PROMPT`, `SYSTEM_PROMPT_10`/`14`,
  `COMBINE_SYSTEM_PROMPT`): explicit rules against city/address/product-name
  as region, bare-species as origin/variety, and inventing/mis-reporting
  non-calendar text as a date.
- 13 new unit tests in `CoffeeMetadataSanitizerTest.kt` (Rule 0 guards +
  `sanitizeDate`), including a regression guard for real non-ISO dates
  (`20.03.2026`) after an initial too-strict regex-only implementation broke
  an existing test — fixed by delegating to `DateParser` instead of
  reinventing format validation.

**Validated on-device, same 9-bag subset**: hallucination 27% → 18%
(-9pp aggregate). Per-field, the three dictionary-guardable fields roughly
**halved**: origin 40%→20%, region 50%→25%, process 40%→20%. variety
12%→0%. roastLevel stayed 0% (already fixed in session 3).
**Did not improve**: tastingNotes (100%→100%, n=1, free-text, no dictionary
guard possible) and roastDate (100%→100% — format validation only catches
malformed dates like "3 months from roast", not well-formed-but-fabricated
ones; the prompt rule against inventing dates did not fully hold on this
harder Q2 subset). **These two remain a follow-up** — likely needs either
accepting this as an inherent free-text/date-plausibility limit, or a
plausibility heuristic (e.g. roastDate should not be far in the future).

**Real infra bug found mid-fix (unrelated to the hallucination fix itself,
but blocked validating it)**: earlier session 3 work had run
`adb shell pm clear com.adsamcik.mindlayer.service.debug` to reset a stuck
consent state, which also wipes that package's model files directory. Only
the NEW package (`com.adsamcik.mindlayer.debug`) got its models restored at
the time — the OLD package didn't, and `FullPipelineBenchmarkTest`/
`MindlayerLlmInferenceProvider` resolve to the OLD package as their "debug
fallback" (a different resolution path than raw `Mindlayer.shared()`, which
resolved to the NEW package). First validation attempt silently produced an
all-fields-MISSING result (engine couldn't init, `Discovered 0 model(s)`)
until this was caught (via logcat, not the gradle output) and all 7 model
files were re-pushed to the OLD package too.

## Session 3 — A/B validation, dependency/consent bugs, multi-turn architecture probe

Commits on `main` (newest first): `6d9bd9f` broadcast dual-package fix,
`3fad0d0` roastLevel vision prompt + sentinel + tastingNotes translate/dedup,
`2db7d45`/`1a5cd01` red-test fix + manifest queries. Mindlayer `main`:
`cacc59d` exposes `Mindlayer.conversation()` on the public interface (PR #207),
`c879850` Dependabot fixes (PR #206).

**A/B results on the curated 6-bag subset** (`postfix-baseline` = current
`main`, i.e. all this session's fixes applied):
- `postfix-baseline`: exactAcc=76% decisionAcc=80% recall=80% **halluc=0%**
  (roastLevel hallucination fix CONFIRMED: was 100% pre-fix, see session 2).
- `stitch` (front+back composite image): exactAcc=77% decisionAcc=82% — **+1-2pp,
  within the >10pp noise guard, NOT a confirmed win.** Not adopted.
- `selfConsistency=3`: **byte-identical** to postfix-baseline — zero effect.
  Likely cause: temperature 0.1 makes the 3 samples too similar to matter;
  would need higher temperature on the re-samples to be useful (no provider
  hook for that currently). Not adopted.
- `knownVocab` (measured session 2 on pre-fix code): still the single
  biggest lever (halluc 25%→0%, roaster 50%→100%, name 33%→66%). **Not yet
  re-measured together with this session's roastLevel/tastingNotes fixes** —
  do that combination next; likely additive but unverified.

**New-samples subset (9 auto-generated Q2-tier bags, real ground truth,
NOT curated) — MAJOR FINDING, not noise (40pp gap, exceeds the >10pp guard):**
exactAcc=36%/56%ceil decisionAcc=68% recall=64% **halluc=27%** — dramatically
worse than the curated 6-bag baseline. Hallucination is **broad, not just
roastLevel**: origin 40%, region 50%, process 40%, expiryDate 42% (all with
denominators of 5-8, not noise). 7 of 9 bags have ≥1 hallucinated field.
roastLevel itself stayed at 0% hallucination (that specific fix holds), but
its exactAcc dropped to 33% (more under- than over-confident on this set).
**Conclusion: the roastLevel-specific fix does not generalize.** There is a
broader "confident-wrong-on-sparse-labels" pattern across concept fields that
needs its own investigation — likely extending the same
never-guess-without-an-explicit-marker discipline (already applied to
roastLevel in `VISION_SYSTEM_PROMPT`) to origin/region/process/dates, or
applying `knownVocab`-style grounding more broadly than just roaster/name.
**RESOLVED in session 4 — see above.**

**Multi-turn conversation architecture probe (explored per user request) —
NEGATIVE RESULT, not adopted:** built `MultiTurnPipelineBenchmarkTest`
(archived, not committed — see below) using `Mindlayer.conversation()`
directly: turn1 = front image + front OCR fused in one call, turn2 = back
image + back OCR as a follow-up turn in the SAME session, turn3 = text-only
self-critique specifically re-checking roastLevel. Result on the same 6
curated bags: turn1=61%/75%halluc, turn2=60%/75%, turn3=60%/75% — **worse
than the production 4-pass pipeline (76%/0%) and flat-to-slightly-down across
turns.** Likely causes: no `knownVocab`-style grounding, one shared system
prompt across turns dilutes per-pass specialization, no per-pass sampling
tuning. **Recommendation: do not pursue multi-turn conversation for
production**; the current one-shot-per-pass + combine architecture remains
better as measured. The test file is archived in the session workspace
(not committed, since it depends on an SDK method not yet in any published
Mindlayer version) for reference if this is revisited after a real SDK bump.


**Two real infra bugs found and fixed along the way (both root-caused, not
just patched):**
1. **Mindlayer SDK gap**: `Conversation`/`ConversationBuilder` existed and
   worked, but `conversation()` was declared only on the `internal
   MindlayerImpl` class, never on the public `Mindlayer` interface that
   `Mindlayer.connect()`/`Mindlayer.shared()` actually return — genuinely
   unreachable from any external consumer, confirmed absent at both current
   HEAD and the published `v1.0.0-alpha.2` tag. Fixed upstream: Mindlayer PR
   #207 (merged, `cacc59d`) adds the interface member (pure additive
   widening, reviewed+approved). **StarlitCoffee still depends on
   `1.0.0-alpha.2`** (the real published version, which predates this fix) —
   a new Mindlayer SDK version must be tagged/published before StarlitCoffee
   can actually use `mindlayer.conversation()` in a committed test.
2. **Debug auto-accept broadcast package mismatch**: `scanBenchmark`'s
   `DebugAutoAcceptReceiver` broadcast only targeted the pre-rename
   `com.adsamcik.mindlayer.service.debug` package. When the post-rename
   `com.adsamcik.mindlayer.debug` package is the actually-bound service (both
   can be installed side by side on a dev device), the broadcast silently
   no-ops against the real active service — consent never gets granted, and
   `awaitConnected` throws "Mindlayer rejected this app" with no indication
   the broadcast itself was the problem. Fixed in `6d9bd9f`: broadcast to
   both package names now (mirrors the existing manifest `<queries>`
   dual-package fix from session 2).

**Operational lesson (session 4, no code change — an environment gotcha):**
a physical Android device connected via USB alongside the emulator got
`installDebug`/`installDebugAndroidTest` installs it should never have
received. **AGP's install tasks target ALL connected authorized devices by
default — `$env:ANDROID_SERIAL` only scopes raw `adb` calls, NOT AGP's own
device enumeration inside `scanBenchmark`/`installDebug`.** Caught via a
`pm list packages` check on the physical device's serial, uninstalled
immediately. If a physical device must stay untouched while benchmarking,
disconnect it or revoke USB debugging authorization for the session —
setting `ANDROID_SERIAL` alone is NOT sufficient.

## Still open / next priorities (updated session 4)
1. **tastingNotes and roastDate hallucination remain unfixed** (both 100%,
   but small n=1-2 on the 9-bag subset — needs a larger sample to confirm
   this isn't just noise). roastDate specifically: format validation can't
   catch a well-formed-but-fabricated date; the prompt rule against inventing
   dates didn't fully hold. Consider a plausibility heuristic (roastDate
   shouldn't be far in the future; expiryDate should postdate roastDate) as a
   next deterministic layer, or accept this as an inherent limit and lower
   priority given it's lower-stakes than origin/region/process.
2. Re-measure `knownVocab` combined with all of session 3+4's fixes
   (roastLevel/tastingNotes-translate/broad-hallucination guards) — not yet
   done together.
3. SDK bump to get `Mindlayer.conversation()` into a real published version —
   only worth doing if multi-turn is revisited; not needed otherwise given
   the negative result in session 3.
4. `-PallBags` full run remains infeasible at current corpus size (77+
   sidecars and growing × ~4-5 min × 3 medians ≈ 15h+); representative
   subsets remain the substitute.
5. Corpus keeps growing live (29→47→53→65→77+ sidecars across sessions,
   now including French/Spanish Q2 batches and an unscored `sk`-locale
   batch) — re-check corpus size before assuming a fixed bag count in any
   future benchmark.

## Session 2 — nine extraction improvements (all implemented)

Commits on `main` (newest first): `9419f28` self-consistency, `ccb8781`
stitch, `ca74f7f` known-vocab, `aa75745` combine-sees-OCR, `2723356` vision
abstention calibration, `7482fa9` tastingNotes phrase comparator, `571f220`
isDecaf default + roastLevel enum-snap.

Validated on the curated 6-bag full-pipeline subset (text-only vs
text+vision+combine), vs the pre-session combined baseline (exactAcc 76%):
- **Combined exactAcc 76→78%, recall 80→82%, decisionAcc 82%** (ceiling 95%).
- **isDecaf 75→100%** (#1 default false), region/variety to 100%, name 33→50,
  roaster 50→66, process +16pp, roastLevel text +enum-snap.
- Remaining hallucination: a single CONFIDENT roastLevel guess on one Q3 bag
  (#7 only suppresses *uncertain* vision — a confident-wrong read is a model
  calibration issue, not a merge-policy one).

Idea-by-idea:
1. **isDecaf=false default** (parseResponse) — done, validated (→100%).
2. **tastingNotes phrase comparator** — done. NOTE: investigating this revealed
   the real defect (below): the text/translate pass leaves cs/it/fr tasting
   notes UNTRANSLATED; the comparator correctly still marks them wrong.
3. **Combine sees raw OCR** — done (name/roaster tie-breaking).
4. **Known-vocab variant** (`-PknownVocab`) — implemented; on-device A/B NOT yet
   run (likely the biggest real-world win — measure next).
5. **Front+back stitch** (`-Pstitch`) — implemented as a benchmark opt-in;
   A/B NOT yet run (stitching halves per-panel resolution — measure before any
   production adoption).
6. **roastLevel enum-snap** (parseResponse) — done ("Light Roast"→"Light").
   Deeper: the SDK's `outputJson` schema+validate+retry IS true structured
   output. My earlier "retry conflicts with the one-shot vision budget" was
   WRONG (see the #2028 repro finding below): `max_retries` is configurable
   (0 valid) AND a 2nd vision inference is recoverable anyway. So
   `outputJson(max_retries=0)` on vision and `>0` on text/combine is viable —
   worth adopting over the deterministic enum-snap.
7. **Vision abstention calibration** — done (production BagVisionPlanner +
   benchmark merge): an uncertain vision read fills a gap but can't clobber.
8. **Self-consistency** (`-PselfConsistency=N`) — implemented (text pass only);
   A/B NOT yet run. Caveat: at temp 0.1 samples barely differ — most useful if
   the re-samples use higher temperature (would need a provider sampling hook).

## NEW real defect discovered — tasting-note translation (RESOLVED — see below)
RESOLVED 2026-07-01 (commit 877dd9e). The translate-first pass did NOT reliably
translate flavour/tasting notes for cs/it/fr bags (it worked for de): text pass
produced "Meruňka"/"mirtillo"/"prune" instead of apricot/blueberry/plum. Fixed in
the TRANSLATE pass (`TRANSLATE_SYSTEM_PROMPT`) — see the "Tasting-note translation
defect — RESOLVED" section lower in this file for details + on-device validation.

## Tasting-note translation defect — RESOLVED 2026-07-01 (commit 877dd9e)
The translate-first pass under-translated cs/it/fr flavour notes ("meruňka"/
"mirtillo"/"prune" instead of apricot/blueberry/plum; de already worked). Fixed by
hardening `TRANSLATE_SYSTEM_PROMPT` in MindlayerLlmInferenceProvider with a
dedicated flavour block (translate every descriptor incl. single Capitalized
tokens, translate by meaning not appearance, false-friend warning, multilingual
examples). Validated on-device against the real Gemma model
(`TranslatePassFlavourInstrumentedTest`, env-gated):
  it "mirtillo, bergamotto, cacao"        -> blueberry, bergamot, cocoa
  fr "prune, chocolat, mandarine"         -> plum, chocolate, mandarin
  cs "meruňka, karamel, krvavý pomeranč"  -> apricot, caramel, blood orange
NOTE: kept purely prompt-side per the design decision that tasting notes are
free-text (the deterministic CoffeeMetadataNormalizer dictionary is deliberately
NOT applied to them — its contains-fallback would false-hit, e.g. blackberry->berry).

### On-device benchmark environment caveats (updated session 3)
- **RESOLVED (session 3, `6d9bd9f`)**: the debug auto-accept broadcast now
  targets both `com.adsamcik.mindlayer.service.debug` (pre-rename) and
  `com.adsamcik.mindlayer.debug` (post-rename) — whichever is actually bound
  now gets consent granted. The underlying applicationId-rename SDK-binding
  gap (#201) is still not fully closed (StarlitCoffee's SDK dependency
  predates the rename), but the manifest `<queries>` entries (session 2) plus
  this broadcast fix are enough for on-device benchmarking to work reliably.
- **RESOLVED (session 2/3)**: the PaddleOCR PP-OCRv5 mobile rec model was
  rebuilt via `scripts/build-paddleocr-models/` (Docker onnx2tf pipeline) —
  the previously-provisioned model had an unresolved `ONNX_LAYERNORMALIZATION`
  custom op LiteRT can't run. Real OCR now works end-to-end (20-29 lines/bag).
- **Gotcha (session 3)**: `adb shell pm clear <mindlayer pkg>` wipes that
  package's `/sdcard/Android/data/<pkg>/files/` model directory too — if you
  ever need to reset Mindlayer's consent state for debugging, re-push all
  model files afterward (`gemma-4-E2B-it.litertlm`, `embedding-gemma-300m-v1.*`,
  `paddleocr-ppocrv5-mobile-*`) and force-stop + relaunch the service.

## Still open (measurement / follow-ups)
- Run the on-device A/Bs for the opt-in variants: `-PknownVocab`, `-Pstitch`,
  `-PselfConsistency=3` (each ~20 min on the curated subset).
- Real-bag ground-truth set + `-PallBags` 3× medians (synthetic-corpus overfit
  risk; treat only >10pp deltas as real).
- roastLevel confident hallucination on Q3 night bags (model calibration).
- Consider adopting the SDK `outputJson` structured-output path for enums.

## LiteRT-LM #2028 — minimal repro investigation (RESOLVED: does NOT reproduce on 0.12.0)
Context: StarlitCoffee's vision pass carries a one-multimodal-inference-per-process
guard (`visionInferenceConsumed`), inherited defensively from Mindlayer's #2028
docs ("the SECOND `Engine.close()`+new `Engine()` in a process SIGSEGVs in
`liblitertlm_jni.so`; the dispatch-delegate registry keeps dangling pointers and
the next init reads freed memory in `magic_number_utils.cc`"). To verify whether
that guard is still needed, a standalone repro was built at
`G:\Github\litertlm-2028-repro` (zip on Desktop: `litertlm-2028-repro.zip`, ~64KB).

It depends ONLY on `com.google.ai.edge.litertlm:litertlm-android:0.12.0` (the SAME
version Mindlayer uses — confirmed in Mindlayer `gradle/libs.versions.toml`), no
wrappers, runs in an isolated `:ml` process, model = `gemma-4-E2B-it.litertlm`, CPU.

**Result (2026-06-30, x86_64 Android-36 emulator): the crash does NOT reproduce.**
Tested, all clean (no `signal 11`, no tombstone, `magic_number_utils.cc` reloads fresh):
- two image inferences on one engine;
- 1× close/recreate (main process AND isolated `:ml` process);
- **5× close/recreate cycles, vision each** (disproves "the 2nd cycle SIGSEGVs");
- **5× close/recreate cycles, TEXT then VISION each** (Mindlayer's multi-pass flow).

Conclusion: the documented close/recreate trigger alone does NOT crash 0.12.0. The
real Mindlayer-stack crash (a confirmed `signal 11` tombstone seen earlier) must
depend on a condition the minimal harness doesn't exercise. Untested candidates:
(a) GPU vision backend with a thermal GPU→CPU **switch** (SwiftShader emulator
can't run GPU), (b) a bound cross-app service receiving images over **binder IPC**,
(c) **warm-slot conversation reuse** with KV-cache eviction (Mindlayer reuses
conversations; the repro makes a fresh one each call). OR it is fixed in 0.12.0.

Implication for StarlitCoffee: the `visionInferenceConsumed` one-shot guard may be
over-conservative for 0.12.0. Do NOT remove it yet — the real crash was Mindlayer-
specific and unisolated. To settle it definitively, reinstall Mindlayer
(`installDebug` in `G:\Github\Mindlayer` + StarlitCoffee), reproduce the multimodal
scan crash, and pull the tombstone (`adb shell run-as ... ` / `dumpsys dropbox` /
`/data/tombstones`) to read the actual native frame. That backtrace is the missing
evidence. The repro app + 2.4GB model are kept installed on the emulator for this.

---

## What is already done (session 1 context)

The on-device coffee-bag scan extraction pipeline was substantially improved and
shipped this session. Relevant commits on `main`:

- `3419cb6` — "Prepare Your Brew" dose-first + setup-aware grind step
- `7eaa3db` — scanned-bag field sanitizer (`CoffeeMetadataNormalizer.sanitizeExtraction`)
- `d72e9b2` — maxTokens 4096→8192 (fixed `input_exceeds_context` starvation)
- `f8ac389` — per-pass LLM telemetry (`LlmPassDiagnostic`)
- `dd7bd85` — `scanBenchmark` Gradle task + `benchmark.instructions.md`
- `dbab732` — moved diagnostics contract to neutral `domain.scandiagnostics` (package-cycle fix)
- `0d13709` — **translate-first extraction** (OCR→English pre-pass, then per-field extract)
- `c9fb414` — bag extraction runs as a foreground service (`dataSync`)
- `031c173` — Settings refactor (Display & dim mode) — separate parallel work

**Translate-first result** (text pass only, 28-bag synthetic corpus vs ground
truth): exactAcc 9%→51%, recall 11%→63%, every field up 20–60pp. Verified
end-to-end on an API 36 emulator.

---

## Why accuracy looks "only ~51%" (read before optimizing)

Two structural facts, both verified in code:

1. **The metric is pessimistic — its ceiling is ~83%, not 100%.**
   `exactAcc = exact / total`, and `total` includes correctly-ABSTAINED cells
   (fields not on the bag that the model correctly left blank). ~17% of all cells
   are correctly-absent, so a *flawless* extractor maxes out near 83%. The honest
   "got present fields exactly right" number is **recall = 63%**. See
   `app/src/sharedTest/kotlin/com/adsamcik/starlitcoffee/test/corpus/QualityReport.kt`
   (metric defs) and `BagFieldScorer.kt` (outcomes).
   - Examples: altitude exactAcc 57% but recall 100%; variety 53% vs 83%; farm 42% vs 70%.

2. **OCR is the real ceiling on messy bags — and the benchmark can't see it.**
   `LlmCorpusBenchmarkTest` scores the **text pass only**, on cached OCR
   fixtures. Same prompt across capture tiers: Q0 (studio) 71% → Q1 51% → Q2 46%
   → Q3 (night/glare) 42% — a 29pp swing driven purely by input quality. The
   model can't extract tokens OCR dropped. The fix is the **vision pass**, which
   the benchmark does not currently exercise.

Genuinely weak fields split two ways:
- Free-text, hard to exact-match, low-stakes (user edits anyway): `name`,
  `roaster`, `tastingNotes`.
- Real model confusion, worth fixing: `roastLevel` (~10/28 wrong), `process`
  (bean-form/decaf leak — e.g. `BOHNEN·EN GRAINS` mis-mapped). Both are
  vision-addressable (marks, layout).

Also: hallucination rose 6%→20% (translate-first abstains less); single runs at
temp 0.1 wobble ±5pp.

---

## Unfinished work (priority order)

### DONE this session (commits on `main`)
- **Item 2 — honest report metrics** (`6344de4`): added `exactAccuracyCeiling`
  (visible/total) and `decisionAccuracy` ((exact+abstained)/total) to
  `QualityReport`; the text report now leads with ceiling / decisionAcc / recall
  / partialRecall / hallucination + a one-line legend. exactAcc is no longer
  misreadable against 100%.
- **Item 1 — full-pipeline benchmark** (`cdec92e`): `FullPipelineBenchmarkTest`
  (one bag per process — the vision pass has a hard one-multimodal-inference-
  per-process budget, LiteRT-LM #2028) runs text→vision→combine for a `bagId`
  arg, scores text-only and the merged output, appends a serialized record;
  `FullPipelineAggregateTest` reduces the JSONL into comparable reports + a
  per-field delta. Drive with `scanBenchmark -PfullPipeline` (curated subset;
  `-PbagIds` / `-PallBags` override). `BagScore`/`FieldScore`/`FieldOutcome` are
  now `@Serializable`.
- **Item 3 — vision per-field contract** (`33456a0`): `VISION_SYSTEM_PROMPT` now
  carries the same explicit per-field definitions as `SYSTEM_PROMPT_14`, keeping
  vision's visual-cue guidance.

**Measured (curated 6-bag subset, text-only vs text+vision+combine):** overall
exactAcc 71→76%, recall 75→80%, decisionAcc 76→79%. Per-field: **roaster +33pp
(50→83%, vision reads the logo and fixes the text pass's name/roaster
confusion)**, region +16pp, variety +16pp. Reports in
`app/build/reports/scan-benchmark/full-pipeline-*.txt`.

### 4. Real-bag validation + noise control (NEXT)
- The 6-bag run is a curated subset of the synthetic corpus. Run the full
  corpus (`scanBenchmark -PfullPipeline -PallBags`, ~3-4 min/bag → ~1.5h) and,
  for noise, 3× medians; treat only >10pp deltas as real.
- Build a small **real-bag** ground-truth set (emulator gallery has real bags)
  so we measure general reliability, not corpus-overfit.

### 5. Weak-field fixes (now measurable via the full-pipeline harness)
- **roastLevel hallucination**: on a Q3 night bag the vision pass turned a correct
  abstention into a guessed roastLevel (the only hallucination in the 6-bag run).
  Consider tightening the vision prompt to abstain harder on roastLevel when no
  mark is readable — but verify on >1 bag first (do not over-fit to n=1).
- **tastingNotes** (33% exact) and **name** (50%) remain weak — largely free-text
  / low-stakes (users edit), lower priority.
- `process` / bean-form leak: re-check on real bags.

### Earlier context (still true)
- `exactAcc` ceiling is structural (~83% on the full corpus, 95% on this clean
  subset): correct abstentions are never "exact". Read against the ceiling.
- OCR is the ceiling on messy bags; the text-only benchmark can't see vision —
  the new full-pipeline benchmark can.

---

## How to run the benchmark (do NOT use connectedAndroidTest)

`connectedAndroidTest` uninstalls the app between runs and wipes the OCR
fixtures. Use the dedicated task:

```powershell
.\gradlew.bat scanBenchmark                # capture OCR fixtures, then benchmark
.\gradlew.bat scanBenchmark -PskipCapture  # reuse fixtures (prompt-only iteration)
```

Report lands in `app\build\reports\scan-benchmark\llm-fixture-quality-report.{txt,json}`.
Full details: `.github/instructions/benchmark.instructions.md`.

Notes:
- Debug app id: `com.adsamcik.starlitcoffee.debug` (test `…debug.test`).
- On-device inference is slow (~30 min/run, ~1–2 LLM calls/bag).
- The `scanBenchmark` task auto-enables the Mindlayer debug auto-accept receiver
  so inference runs unattended.
- Use the `android-emulator-mcp` server for any on-device interaction (claim the
  device first).

## Known unrelated failing test
`CorpusMetadataReadinessTest > duplicate barcode values are explicitly marked as
collisions` fails on a clean baseline — pre-existing, not from this work.

## Cross-repo note
The Mindlayer SDK lives at `G:\Github\Mindlayer`. See its own `.incomplete.md`
for one open item (a stale local branch awaiting a push/merge/leave decision).
